apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cadvisor-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: cadvisor
  endpoints:
    - port: 8080
      path: /metrics
      interval: 30s
