apiVersion: v1
kind: Service
metadata:
  name: node-exporter-service
spec:
  type: NodePort
  selector:
    app: node-exporter
  ports:
    - name: http-metrics
      port: 9100
      targetPort: 9100
      nodePort: 30100  # Tu peux choisir un port entre 30000 et 32767
