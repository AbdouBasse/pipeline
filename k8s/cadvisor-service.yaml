apiVersion: v1
kind: Service
metadata:
  name: cadvisor-service
  labels:
    app: cadvisor
spec:
  selector:
    app: cadvisor
  ports:
    - name: http-metrics   # 👈 ce nom est important
      port: 8080
      targetPort: 8080
