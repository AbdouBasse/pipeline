apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics-service
spec:
  type: NodePort
  selector:
    app: kube-state-metrics
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 8080
      nodePort: 30081
