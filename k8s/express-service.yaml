apiVersion: v1
kind: Service
metadata:
  name: express-service
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30500  # Port accessible en local
